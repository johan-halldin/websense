# PostgreSQL/TimescaleDB connection used by apps/server, apps/ingest, and db/migrate.js
DATABASE_URL=postgres://postgres:postgres@localhost:5433/websense

# apps/server
PORT=3002

# apps/ingest fetches mesh ping data for whichever cities are in the
# `cities` table (managed via POST/DELETE /api/cities) - no env config needed.
